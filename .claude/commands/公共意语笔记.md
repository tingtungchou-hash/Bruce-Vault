description: "整理公共初级意大利语课程笔记：把课上随手记的原始材料按 词汇 → 课文（对话 / 例句） → 语法 的顺序重排，按语义与功能分组、统一标题层级与列表符号、为词汇和例句补全中文、修正明显笔误；frontmatter 与导航栏原样不动"
steps:
  - "判定目标笔记：
     - 默认处理当前会话 `<linked_note>` 指向的笔记，或 `<editor_selection>` 所属的笔记。
     - 用户用 `@文件` 指定时，以指定者为准。
     - 两者皆无且用户未指定时，先向用户确认，不要猜。
     - 预期路径形如 `Areas/学业/课程/<学期>/公共初级意大利语/<第N课> <日期> 公初意.md`。若目标不在此文件夹，提示用户这可能不是本 skill 的适用对象，确认后再继续。"

  - "通读原文并保护存量元信息：
     - 先完整读一遍原文，判断哪些是词汇、哪些是课文、哪些是语法，再动手，不要边读边改。
     - frontmatter（title / created / course / professor / semester / tags）与导航栏（返回导航 … 本课教材）原样保留，不重写、不补槽位、不调整顺序。
     - 若发现导航栏缺“下一课”而该课并非末篇，只在汇报中提示，不擅自改动。"

  - "按内容重排为三大节（顺序固定，不可颠倒）：
     - `# 词汇 lessico`
     - `# 课文 testi`（含对话、例句、短文、句型）
     - `# 语法 grammatica`
     原文使用 `# 对话`、`# 例句`、`# Unità X` 等标题的，一律并入“课文”节，作为节内的分组小标题或列表组。
     原文缺其中某节时不硬造空节，保留已有的节即可。
     无法归入三节的专题按以下规则安置：
     - 发音 / 拼写 / 重音规则 → 并入“语法”节，作 `##` 小节（如 `## 发音：S 与 Z`）。
     - 地理、文化、地图图片等 → 单列为 `# 其他` 节，排在“语法”之后。
     若笔记中语法与其他内容都为空，则不写这两节。"

  - "整理词汇：
     - 按语义分组，组名用“中文 + 意大利语术语”，如 物品与日常、身体 il corpo、性质与形容词、连词 congiunzioni、职业与工作 professioni、称谓 titoli、地名、数字 numeri、动词。
     - 每组内每行一条，格式为 `意大利语 中文`（词与中文之间一个空格，不加冒号、不加破折号、不加粗）。
     - 原文已有中文的照录；原文没有中文的补标准释义。补中文只补该词在课文语境中的常用义，不做多义项罗列。
     - 原文标注的性数、复数、缩写说明（如 `f.`、`m.`、`21 ventuno`）原样保留。
     - 同一词在原文多处出现时合并到首次出现处，不重复列条。
     - 疑似拼写错误的词按正确拼写收录，并在汇报中列出原形与正形。"

  - "整理课文：
     - 按功能或场景分组，组名用“中文 + 意大利语术语”，如 问候与寒暄、年龄与家庭、所属表达、询问职业与来历、状态程度。
     - 对话原文完整保留、保留原有语序与断句；说话人标注、教材的人物设定标注（人名、颜色标记、角色说明）原样保留。
     - 中文补在何处：短句、短语、词组一律行内跟在后面（`意大利语 中文`）；成段的对话、短剧台词不打散原文，对话整体保留后在下方另起一段“译文”，逐行对应。原文已有中文的照录。
     - 修正明显笔误（拼写、重音、冠词、介词、动词变位、大小写），但不得改写表达方式、不得增删信息、不得合并或拆分说话人的话轮。
     - 原文中标注的语法提示（如 `(= here)`）保留，可补中文说明。"

  - "整理语法：
     - 每个语法点一个 `##` 小节，标题用“中文名称 + 意大利语术语”，如 `## 疑问词 parole interrogative`、`## 冠词 articoli`。
     - 动词变位一律分行列举，每人称一行（io / tu / lui, lei, Lei / noi / voi / loro），不用表格、不用代码块。
     - 只整理原始笔记中已记录的人称与时态，不擅自补全未记录的人称；变位明显残缺时在汇报中提示，由用户自行决定是否补。
     - 例句格式为 `意大利语 中文`，译句紧跟在例句后。
     - 术语一律中文在前、意大利语原文在后（如 省音 elisione、定冠词 articolo determinativo）。
     - 原文混入语法节的零散词条（如误放在此的疑问词表、数字）应移回“词汇”或归入对应语法点，不留悬空条目。"

  - "统一结构与分段符号（增删依据）：
     - 标题层级：`#` 只用于三大节与其他专题节；分组名与语法点用 `##`；不再引入更深的标题，更细的层次用缩进列表表达。
     - 列表符号统一用 `-`；除表达明确的从属关系外不嵌套第二层。
     - 删除无意义的行：连续空行、孤立分隔符、重复行、误触产生的 markdown 强调标记。
     - 删除原文标题里无实义的后缀或编号噪声，保留有信息量的标题。
     - 全程不使用表格、不使用代码块、不使用 emoji、不代为加粗。
     - 中文引号一律用 “”，不使用方角引号；意大利语原文中的引号保留原样。"

  - "本 skill 的效力边界：
     - 本 skill 是 CLAUDE.md“写入笔记时不代为分节、不代为加粗”一条的显式例外 —— 用户点名要求整理，故允许分组、补中文、调整标题层级。
     - 但“不代为加粗”与“不使用表格”在任何情况下都不得违反。
     - 不得自行编造原文没有的知识点、例句或语法规则；补中文只限词汇释义与例句翻译。
     - 不动笔记正文以外的任何文件；确需改动上级导航（如补“下一课”链接）时，先问用户。"

  - "输出质量检查（返回用户前必须执行，沿用 CLAUDE.md 后验规则）：
     1. Wikilink 括号配对：逐行确认 `[[` 与 `]]` 数量相等；无单括号形式的坏链（排除合法外链）；无三个及以上连续的右括号。
     2. 意大利语音符完整性抽查：à è é ì ò ù 与省音撇号（l'、un'、anch'io、c'è、dov'è）保留，未被误改。
     3. 格式合规抽查：文件中不出现表格、不出现加粗标记、不出现 emoji、不出现方角引号。
     4. 若文件含图片嵌入或指向本课教材的链接，校验目标文件存在，报告断链。
     发现即修正，再输出。"

  - "汇报：一句话说清 —— 整理了哪一课、重排出哪几个 `#` 节、补了哪些中文、修正了哪些笔误（逐条列出 原形 → 正形）、以及哪些地方存疑未动（残缺变位、缺失的导航槽位等）。"
