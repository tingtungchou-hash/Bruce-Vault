description: "根据 Lewis & Short 词典自动生成拉丁语单词笔记，包含基本信息、释义、词源、关联词汇"
steps:
  - "接收用户指定的拉丁语单词（如 'amo'），或从当前笔记/上下文中提取目标单词"
  - "**@ 调用规则（强制）**: 若用户消息中使用 `@` 语法（如 `/整理单词 @ insidiae, negotium, obeo`），则**仅处理 `@` 后列出的单词**，不得自行扩展或添加任何额外词汇。无论上下文中有多少其他候选词，一律忽略。若没有 `@` 语法，则可从当前笔记或上下文中提取目标单词。"
  - "在 Resources/lewis-short-json-master/ 下按首字母确定对应的 JSON 文件（如 amo -> ls_A.json），搜索匹配的 entry：
     - 优先匹配 `key` 字段（注意大小写，'A' 与 'a' 不同）
     - 若精确匹配失败，尝试大小写不敏感的匹配
     - 若仍失败，告知用户该词不在 Lewis & Short 词典中"
  - "从 JSON entry 中提取以下信息：
     - `title_orthography`：标准拼写（含长音符号）
     - `title_genitive`：属格形式（名词）
     - `gender`：性（M/F/N）
     - `declension`：变格法编号
     - `part_of_speech`：词性标记
     - `main_notes`：词源和用法说明（提取其中的词源信息，忽略语法引用细节）
     - `senses`：嵌套的释义数组（提取顶层及次级义项，保留层级关系）"
  - "根据词性判断笔记标题格式：
     - 名词：`{title_orthography}, {title_genitive} {gender_label}.`（如 `cōpia, -ae f.`）
     - 形容词：`{title_orthography} adj.`（如 `grātus, -a, -um adj.`）
     - 动词：提取主要词形变化构成标题（如 `amō, amāre, amāvī, amātus 1.`）
       来源：从 `main_notes` 开头提取（如 'ămo, āvi, ātum, 1' -> 'amō, amāre, amāvī, amātus 1.'）
     - 介词/其他：`{title_orthography}` + 词性说明（如 `prō (prep. + abl.)`）"
  - "在 Areas/语言/拉丁语/词库/ 下创建新笔记，文件名为 `{标题}.md`
     采用以下模板结构："
  - "YAML frontmatter：
     ---
     created: {{today}}
     language: 拉丁语
     tags: [vocabulary, language/latin]
     status: seedling
     ---"
  - "笔记正文结构：
     # {标题}

     ## 基本信息
     - **语言**: 拉丁语
     - **词性**: {从 part_of_speech 和 gender/declension 推算的中文词性说明，如 'n. f. (第一变格法)'}
     - **词源**: {从 main_notes 提取的词源信息，如 Sanscrit/希腊语同源词；若无则留空}

     ## 释义
     {从 senses 数组提取主要义项，按层级编号：
      - 顶层义项用数字 1. 2. 3. ...
      - 次级义项用缩进字母 a. b. c. ...
      - 跳过纯引用/文献列举的细节
      - 保留关键语义区分
      - 译文用英文（与现有笔记一致）}

     ## 关联词汇
     {若有同源词、派生词，以 wikilink 形式列出}

     ## 复习提示
     （留空）"
  - "创建文件后，执行输出质量检查：
     1. 检查 Wikilink 括号是否配对
     2. 检查长音符号（ā, ē, ī, ō, ū）是否完整保留
     3. 核对生成的释义是否与 Lewis & Short 原始条目一致"
